<h1>{{title}}</h1>
<h1> Estudiantes</h1>
    <p >
        <div class="container">
            <h3><a href="/estudiante/create">Crear Estudiante</a></h3>
            <h1>Lista de Estudiantes</h1>
            {{#if estudiantes }}
                <table class="table table-hover">
                    <thead>
                        <tr>
                            <th>Codigo</th>
                            <th>Nombre</th>
                            <th>Escala</th>
                            <th>Promedio</th>
                        </tr>
                    </thead>
                    <tbody>
                        {{# each estudiantes }}
                        <tr>
                            <td><a href="/estudiante/show/{{ _id }}">{{ codigo }}</a></td>
                            <td>{{ nombre }}</td>
                            <td>{{ escala }}</td>
                            <td>{{ promedio }}</td>
                        </tr>
                        {{/each}}
                    </tbody>
                </table>
            {{~^~}}
                <div>Estudiantes no encontrados.</div>
            {{/if}}
        </div>
    </p>